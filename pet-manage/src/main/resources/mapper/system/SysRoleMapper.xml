<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scnu.mapper.system.SysRoleMapper">
    <sql id="Base_Column_List">
        role_id,role_name,status,create_by,create_time,remark
    </sql>

    <insert id="add" parameterType="com.scnu.entity.system.SysRole" useGeneratedKeys="true">
        insert into sys_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="roleId != null">role_id,</if>
            <if test="roleName != null">role_name,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="roleId != null">#{roleId},</if>
            <if test="roleName != null">#{roleName},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.scnu.entity.system.SysRole">
        update sys_role
        <set>
            <if test="roleName != null">
                role_name = #{roleName},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="createBy != null">
                create_by = #{createBy},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
        where role_id = #{roleId}
    </update>

    <delete id="deleteById">
        delete from sys_role
        where  role_id = #{roleId}
    </delete>

    <select id="selectById" resultType="com.scnu.entity.system.SysRole">
        select
        <include refid="Base_Column_List" />
        from sys_role
        where role_id = #{roleId}
    </select>

    <select id="selectAll" resultType="com.scnu.entity.system.SysRole">
        select
        <include refid="Base_Column_List" />
        from sys_role
        <where>
            <if test="roleId != null"> and role_id = #{roleId}</if>
            <if test="roleName != null"> and role_name like concat('%', #{roleName}, '%')</if>
            <if test="status != null"> and status = #{status}</if>
        </where>
        order by role_id
    </select>

    <select id="selectUserRole" resultType="com.scnu.entity.system.SysRole">
        select role.*
        from sys_role role
                 inner join sys_user_role sur on role.role_id = sur.role_id
        where sur.user_id = #{userId}
    </select>
</mapper>